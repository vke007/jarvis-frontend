[build]
builder = "nixpacks"

[deploy]
# Remove startCommand here so Railway uses your Procfile
healthcheckPath = "/api/auth/verify" 
healthcheckTimeout = 100

[environment]
PYTHON_VERSION = "3.12.10"
